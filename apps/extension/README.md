# HyperlinkVR Browser Extension

This extension runs the whole HyperlinkVR application. It invokes the VR host, manages authentication, exposes an API to the JavaScript globals to allow websites to talk to the VR host, and injects user input from the VR session.

It uses [wxt](https://wxt.dev/) for a Vite-powered, React-compatible extension build system.
